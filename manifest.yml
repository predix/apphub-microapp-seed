---
applications:
  - name: apphub-microapp-seed
    buildpack: https://github.com/cloudfoundry/heroku-buildpack-nodejs.git
    memory: 512MB
    stack: cflinuxfs2
    instances: 2
    path: .
    command: node server
    services:
      #- my_uaa_instance
      - test_uaa_instance
    env:
      NODE_ENV: 'production'
      DEBUG: '*'
